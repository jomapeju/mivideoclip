/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes poner otras opciones si las necesitas.
  // El App Router (src/app) ya se detecta automáticamente.
};

module.exports = nextConfig;
